{
  "name": "typed-factorio",
  "version": "0.3.0",
  "description": "Featureful typescript definitions for the the Factorio modding lua api.",
  "keywords": [
    "factorio",
    "types",
    "typescript-to-lua",
    "tstl"
  ],
  "repository": "https://github.com/GlassBricks/typed-factorio",
  "license": "MIT",
  "files": [
    "/*.d.ts",
    "generated/*.d.ts",
    "common/*.d.ts"
  ],
  "homepage": "https://github.com/GlassBricks/typed-factorio#readme",
  "scripts": {
    "generate": "ts-node scripts/generate.ts",
    "generate:dev": "ts-node scripts/generate.ts dev",
    "pretest": "yarn run generate",
    "test": "jest",
    "prepublishOnly": "yarn run test"
  },
  "dependencies": {
    "lua-types": "^2.10.1",
    "typescript-to-lua": "^0.42.0"
  },
  "devDependencies": {
    "@types/concurrently": "^6.2.1",
    "@types/jest": "^26.0.24",
    "@types/node": "^16.4.13",
    "concurrently": "^6.2.1",
    "jest": "^27.0.6",
    "prettier": "^2.3.2",
    "ts-jest": "^27.0.4",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  }
}
